repo_type: svc
owners:
  team: architecture-platform
commands:
  check: make check
  lint: make style
  test: make audit
risk:
  external_writes: false
  secrets_required: false
  protected_paths:
    - docs/**
agent_entrypoints:
  default_task: make check
